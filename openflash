#!/bin/bash

FILES=$(lsof -M -n -p $(pidof plugin-container | tr ' ' ',') | fgrep '/tmp/Flash' | sed 's?\([0-9]\)[a-zA-Z]?\1?g' | awk '{ printf "/proc/%s/fd/%s ",$2,$4; }')
echo $FILES
vlc $FILES
exit $?
